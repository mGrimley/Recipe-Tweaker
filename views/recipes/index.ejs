<%- include('../partials/header') %>

<div class="flex-container">
  <h1 class="title-card"><%= title %></h1>
  <% recipes.forEach(function(r) { %>
    <div class="recipe-card"><%= r.title %></div>
  <% }) %>
</div>
  
<%- include('../partials/footer') %>